# PLPBasicGitAssignment

I have created a local repo in machine, same as in my github account.
$ git cd Desktop
mkdir PLPBasicGitAssignment
git init 
link the local with the remote using git remote add orign https://github.com/Lasthope9/PLPBasicGitAssignment.git
and verify with git remote -v
i created a text file inside with touch hello.txt
i stage the file with git add hello.txt
i commit -m "add hello.txt greeeting"
i git push -u orign main 
